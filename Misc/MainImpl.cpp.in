#include "Polly.hpp"
#include "${game_header_file}"

#include <SDL3/SDL_main.h>

int main(int /*argc*/, char* /*argv*/[])
{
    Game::setDefaultTitleAndCompanyName("${game_display_name}", "${company}");
    Game::run<${target_name}>();
    return EXIT_SUCCESS;
}
